package com.example.demo.api.model;

/**
 * Should match the role strings available in the db (role table)
 */
public interface RoleConstants {
    String ADMIN = "ADMIN";
    String USER = "USER";
}
